# Database URL — override via .env.local in real deployments.
# This is the local docker-compose Postgres (see docker-compose.yml).
# `.env` is gitignored; CI sets its own DATABASE_URL.
DATABASE_URL="postgresql://pm:pm@localhost:5432/passwordmanager?schema=public"