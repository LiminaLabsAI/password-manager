---
type: Changelog
---

# Changelog — 2026-07

> One line per change, append-only. Newest at bottom of its month.

## 2026-07-06

- **Phase 0 started** — Bootstrap Security Core. Branch `phase-0-bootstrap-security-core` created. Plan: 4 groups, sequential (scaffold → data layer → crypto primitives → auth+round-trip → verification).
- **Phase 0 Group 0** — Scaffolded Next.js 16 + TS + Tailwind v3 + Vitest + ESLint + CI workflow (Postgres 16 service).
- **Phase 0 Group 1** — Prisma 6 + Postgres; `User` + `Vault` schema; initial migration generated via `migrate diff`.
- **Phase 0 Group 2** — libsodium-wrappers-sumo crypto: Argon2id KDF → (vault key, auth hash) via `crypto_kdf`; XChaCha20-Poly1305 encrypt/decrypt; 17 unit tests.
- **Phase 0 Group 3** — Auth API (`/api/signup`, `/api/login`+`/challenge`, `/api/vault` GET/PUT with last-write-wins); client pages `/signup`, `/login`, `/vault`; master password never leaves browser.
- **Phase 0 Group 4** — Smoke test (`tests/smoke/phase-0-roundtrip.test.ts`) against live Postgres: full round-trip + two security invariants (no master/keys over network or in storage; auth hash cannot produce vault key). 18/18 tests pass.
- **v0.1.0 released** — Phase 0 merged to `main` and tagged `v0.1.0`. CI green (run 28812300910). npm publish deferred by user.
- **Project founded** — `docs: found password-manager — vision, roadmap, Phase 0` (root commit `aacb2e6`).